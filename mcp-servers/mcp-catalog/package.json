{
  "name": "mcp-catalog",
  "version": "1.0.0",
  "description": "Unified MCP server catalog - discover, stack, and validate MCP servers (merged from mcp-registry + mcp-stack-curator)",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.7.0"
  }
}
